Markdown
# Deployment Pipeline: Supabase Service-Role Auth + Cloudflare Edge Secret Architecture

## Overview
All sensitive backend environment variables are now securely stored in Cloudflare Workers Secrets (encrypted at rest) rather than cleartext configuration. Public, non-sensitive values remain in `wrangler.toml` as `[vars]`. This architecture ensures:

- **Zero git exposure**: Credentials are never committed to version control
- **Encrypted at edge**: Secrets are encrypted in Cloudflare's secure vault
- **Runtime injection**: Secrets are injected into the worker context at runtime via `c.env.SECRET_NAME`

## Provisioned Secrets

The following sensitive credentials have been uploaded to Cloudflare Workers Secrets:

| Secret Name | Purpose | Status |
|---|---|---|
| `SUPABASE_SERVICE_ROLE_KEY` | Backend Supabase authentication (admin operations) | ✅ Encrypted |
| `SUPABASE_ANON_KEY` | Public Supabase client key | ✅ Encrypted |
| `RESEND_API_KEY` | Email service API credentials | ✅ Encrypted |
| `FLUTTERWAVE_PUBLIC_KEY` | Payment processor public key | ✅ Encrypted |
| `FLUTTERWAVE_SECRET_KEY` | Payment processor secret key | ✅ Encrypted |
| `FLUTTERWAVE_ENCRYPTION_KEY` | Payment encryption credentials | ✅ Encrypted |
| `FLUTTERWAVE_WEBHOOK_SECRET` | Webhook authentication token | ✅ Encrypted |
| `REDIS_URL` | Redis cache connection with auth | ✅ Encrypted |

## Public Configuration (wrangler.toml)

```toml
[vars]
FRONTEND_URL = "https://c560abfb.stanley-portfolio.pages.dev"
SUPABASE_URL = "https://kcxeyusgtgblaihsasbd.supabase.co"
AdminEmail = "owarieta24@gmail.com"
SenderEmail = "no-reply@stanleyowarieta.com"
PaymentLogo = "https://c560abfb.stanley-portfolio.pages.dev/stan-pic.webp"
```

## Adding New Secrets

To provision additional secrets, use the Wrangler CLI:

```bash
echo "your-secret-value" | npx wrangler secret put SECRET_NAME
```

## Accessing Secrets in Code

Within Hono middleware and handlers, access secrets via the context object:

```typescript
const supabaseKey = c.env.SUPABASE_SERVICE_ROLE_KEY;
const resendApiKey = c.env.RESEND_API_KEY;
```

---

# Architectural Sync: Supabase Database Schema Alignment & Bug Fixes

We are experiencing 500 Internal Server Errors because our TypeScript service layer properties do not match our Supabase PostgreSQL table structures. We need to unify our schemas and fix the rate limiter logic.

## 1. DATABASE SCHEMA CORRECTIONS (Execute in Supabase SQL Editor First)
Please ensure our Supabase Postgres tables support the columns our codebase expects. Run this migration script to synchronize the database structure:

```sql
-- Fix contact_messages table column names
ALTER TABLE contact_messages ADD COLUMN IF NOT EXISTS full_name text;
ALTER TABLE contact_messages ADD COLUMN IF NOT EXISTS phone_number text;

-- Fix payments table column names
ALTER TABLE payments ADD COLUMN IF NOT EXISTS balance_due numeric(10, 2) DEFAULT 0.00;
ALTER TABLE payments ADD COLUMN IF NOT EXISTS amount_paid numeric(10, 2) DEFAULT 0.00;

-- Fix rate_limit_log table schema for Hono Edge compatibility
CREATE TABLE IF NOT EXISTS rate_limit_log (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    client_key text NOT NULL,
    tokens_used int DEFAULT 1,
    last_request timestamp with time zone DEFAULT timezone('utc'::text, now()) NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_rate_limit_client ON rate_limit_log(client_key);
2. REFACTORING TASKS FOR THE AGENT
Task A: Fix Rate Limiter Database Errors (src/middleware/rateLimiter.ts)
The rate limiter is throwing Rate limiter DB error.

Inspect how it interacts with Supabase. Ensure that when querying or updating rate_limit_log, it correctly uses c.env.SUPABASE_URL and c.env.SUPABASE_ANON_KEY or SUPABASE_SERVICE_ROLE_KEY.

Wrap the rate limiter database calls inside a standard try/catch block. If it fails, log the error clearly but ensure it handles or gracefully defaults rather than cascading into a hard server crash if possible.

Task B: Align Contact Form Service Column Mapping (src/services/contactService.ts)
The error indicates: Could not find the 'full_name' column of 'contact_messages'.

Open src/services/contactService.ts. Ensure that the object keys passed into .insert({...}) perfectly match the columns available in our Supabase table (name or full_name, email, subject, message). Adjust either the object layout or verify it maps to the updated database columns.

Task C: Align Payment Intent Service Column Mapping (src/services/paymentService.ts)
The error indicates: Could not find the 'balance_due' column of 'payments'.

Open src/services/paymentService.ts and inspect where it hits .insert() or .update(). Ensure the values map cleanly to the updated database schemas (amount, balance_due, reference_id, etc.).

3. VERIFICATION STEP
After making these changes, verify that the project builds correctly and monitor the incoming POST /api/v1/contact and POST /api/v1/payment/initiate routes to confirm they respond with 200 OK or 201 Created.